
  # Generate HTML

  This is a code bundle for Generate HTML. The original project is available at https://www.figma.com/design/UTS37nAviXNtsH6pEOkmPM/Generate-HTML.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  